{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "bundle": "esbuild ./app.js --bundle --format=esm --platform=browser --define:global=globalThis --outfile=./bundle.js",
    "copy-z3": "mkdir -p ./z3 && cp ./node_modules/z3-solver/build/z3-built.{js,wasm} ./z3/",
    "build": "npm run bundle && npm run copy-z3",
    "start": "node ./serve.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "z3-solver": "^4.15.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
